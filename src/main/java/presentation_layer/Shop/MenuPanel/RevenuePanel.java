package presentation_layer.Shop.MenuPanel;

import javax.swing.*;

public class RevenuePanel extends JPanel {
    public RevenuePanel(String id) {
        JLabel label = new JLabel("Revenue Panel - ID: " + id);
        add(label);
    }
}
