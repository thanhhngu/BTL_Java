package presentation_layer.Shop.MenuPanel;

import javax.swing.*;

public class ConfirmPanel extends JPanel {
    public ConfirmPanel(String id) {
        JLabel label = new JLabel("Confirm Panel - ID: " + id);
        add(label);


    }

}
