package presentation_layer.Shop.MenuPanel;

import javax.swing.*;

public class DeliveredPanel extends JPanel {
    public DeliveredPanel(String id) {
        JLabel label = new JLabel("Delivered Panel - ID: " + id);
        add(label);
    }
}
